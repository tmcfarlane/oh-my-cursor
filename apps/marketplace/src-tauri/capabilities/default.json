{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window: core + native folder dialog.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
